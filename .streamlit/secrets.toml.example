# Streamlit Secrets Configuration Example
# Copy this file to secrets.toml and fill in your actual values
# DO NOT commit the actual secrets.toml file to version control

# OpenAI API Configuration (Required)
OPENAI_API_KEY = "your-openai-api-key-here"

# Gemini API Configuration (Optional, but recommended for 20x cost savings)
GEMINI_API_KEY = "your-gemini-api-key-here"

# Google Cloud Configuration for Vertex AI Search (Required for Vertex AI Search)
# Get these values from your Google Cloud Console
GCP_PROJECT_ID = "your-gcp-project-id"
GCP_LOCATION = "global"  # or your preferred region like "us-central1"
GCP_DATA_STORE_ID = "cardgpt-engine_1752662874226"  # Your actual data store ID

# Note: Make sure you have Google Cloud authentication set up
# You can use: gcloud auth application-default login
# Or set up a service account key file